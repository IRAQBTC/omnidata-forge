# Copy this to `.env` and fill in real random values before `docker compose up`.
# Generate secrets with: python3 -c "import secrets; print(secrets.token_hex(32))"

POSTGRES_PASSWORD=change-me
JWT_SECRET=change-me-32-bytes-min
AUDIT_KEY=change-me-a-DIFFERENT-32-bytes-min
